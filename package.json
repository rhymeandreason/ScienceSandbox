{
  "name": "sciencesandbox",
  "version": "0.0.0",
  "private": true,
  "description": "Root manifest for the serverless functions in api/. The lesson pages still have no build step and no dependencies; this exists so api/_providers/ can reach a model SDK, locally and on Vercel. Both SDKs are installed and only the configured one is ever required. The Neon driver is what the tutor logs through, and is a no-op without DATABASE_URL.",
  "scripts": {
    "serve": "node demos/tools/dev-server.js",
    "check:ask": "node demos/ask/check-ask.js",
    "db": "node demos/tools/db.js"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.70.0",
    "@google/genai": "^2.18.0",
    "@neondatabase/serverless": "^1.1.0"
  }
}
