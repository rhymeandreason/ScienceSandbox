# Generated by tools/fold2pdb.js — 2.78 MB, six times the .bin it comes from,
# and not one number that is not already in folding/data/1VII.fold.bin.
# This repo publishes from the working tree; regenerate rather than commit.
#
# 2HHB.pdb is FETCHED, not generated (tools/fetch-hemoglobin.js), and ignored
# for the same reason: it is 443 KB of scratch. If hemoglobin is adopted it
# moves to pdb/ and gets committed, because a LESSON must not stop working
# because a school's network blocks a domain — the argument that already keeps
# pdb/1LYZ.pdb and pdb/1IGT.pdb in the tree.
data/*.pdb
