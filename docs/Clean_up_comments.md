# Clean up comments

Look through this file's comments and clean up information that's no longer relevant. Also reduce redundancies to make it more concise. Comments should be written in active voice, not document the current relevant information, not a chain of past work getting here. Examples:

Before:
THE PANEL DOES NOT RE-TEACH THE LEVELS. The captions say what each
level is, in sync with the thing the student is watching, and a
panel that repeats them is 350 words of the same lesson told worse.
What is left is the part no caption can carry: where this structure
came from, and what the animation is faking.

EVERY NUMBER HERE IS PULLED FROM THE DATA — see fillPanelNumbers(). Type one into the prose and it becomes a claim nothing can check.

After:
THE PANEL DOES NOT RE-TEACH THE LEVELS. 
This is supporting information: where this structure
came from, and what the animation is faking.

EVERY NUMBER HERE IS PULLED FROM THE DATA — see fillPanelNumbers(). Don’t type static numbers into the prose.


Before:
The prose no longer states either number — it reads them (see
fillPanelNumbers), so neither can disagree with the file and neither
needs guarding. This throw is about the LESSON, not the text: the
close-up window, the arrival windows and the camera were all tuned
against this chain, and a different one would render plausibly and
be staged wrong.

After:
This throw is about the LESSON, not the text: the
close-up window, the arrival windows and the camera were all tuned
against this chain, and a different one would render plausibly and
be staged wrong.
