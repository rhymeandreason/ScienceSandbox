# Clean up comments

Look through this file's comments and clean up information that's no longer relevant. Only keep a history-shaped comment if it records a live trap. Goal is to reduce redundancies to make it more concise. Comments should be written in active voice and document the current relevant information for moving forward, not a chain of past work getting here. Examples:

**Before:** TWO CHANNELS, AND THEY SAY DIFFERENT THINGS.

**After:** TWO CHANNELS

**Before:** Each dot is one molecule, and it **flips colour when it drifts through an enzyme** — that is the reaction happening. A dot's **speed is its energy**, knocked about at random. A dot **glows when it has enough to react** — so a glowing one converts the moment it reaches an enzyme, and a dull one drifts straight through.

**After:** Each dot is one molecule. A dot’s speed is its energy, determined randomly. A dot glows where it has enough energy to react. When a glowing dot drifts through an enzyme, it converts (flips color).

**Before:** THE PANEL DOES NOT RE-TEACH THE LEVELS. The captions say what each level is, in sync with the thing the student is watching, and a panel that repeats them is 350 words of the same lesson told worse. What is left is the part no caption can carry: where this structure came from, and what the animation is faking.

EVERY NUMBER HERE IS PULLED FROM THE DATA — see fillPanelNumbers(). Type one into the prose and it becomes a claim nothing can check.

**After:** THE PANEL DOES NOT RE-TEACH THE LEVELS. This is supporting information: where this structure came from, and what the animation is faking.

EVERY NUMBER HERE IS PULLED FROM THE DATA — see fillPanelNumbers(). Don’t type static numbers into the prose.

**Before:** The prose no longer states either number — it reads them (see fillPanelNumbers), so neither can disagree with the file and neither needs guarding. This throw is about the LESSON, not the text: the close-up window, the arrival windows and the camera were all tuned against this chain, and a different one would render plausibly and be staged wrong.

**After:** This throw is about the LESSON, not the text: the close-up window, the arrival windows and the camera were all tuned against this chain, and a different one would render plausibly and be staged wrong.
